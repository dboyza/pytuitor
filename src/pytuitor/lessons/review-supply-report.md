## Practice: unique invitations
Read a line of names separated by spaces.
Store each name once in a list called `guests`, preserving its first appearance.
Use `name not in guests` before appending.
Print the number of guests.
For `Ada Lin Ada`, the list is `["Ada", "Lin"]` and the printed count is `2`.
An empty line produces an empty list and prints `0`.
