## Practice: a limited counter
Define `Counter(limit)` with an initial `.value` of zero.
Its `advance()` method increases value by one until it reaches the nonnegative integer limit, then keeps it unchanged.
Return the current value from every call.
A limit of zero always returns zero.
Different instances must not share their values.
Use `__init__` for instance attributes and an `if` inside the method.
